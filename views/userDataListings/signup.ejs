<% layout("/layouts/boilerplate", { hideFooter: true }) %>

<!-- Custom Styling for Sign Up Page -->
<style>
  
  /* Make page take full height and allow flexible layout */
  html, body {
    height: 100%;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
  }

  body {
    font-family: 'Segoe UI', sans-serif;
    color: #fff;
    position: relative;
    overflow-x: hidden;
    flex: 1;
  }

  main {
    flex: 1;
    display: flex;
    flex-direction: column;
  }

  /* Background image with blur and dim overlay */
  body::before {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    background: url('/image/background.jpg') no-repeat center center fixed;
    background-size: cover;
    filter: blur(5px) brightness(0.6);
    z-index: -2;
  }
.signup-box {
  background-color: rgba(255, 255, 255, 0.1);
  padding: 2rem;
  border-radius: 16px;
  backdrop-filter: blur(6px);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
  width: 100%;
  max-width: 500px;

  transform: translateX(81%); /* Slight shift for large screens */
}

/* ✅ Reset transform for mobile */
@media (max-width: 768px) {
  .signup-box {
    transform: none; /* no shift on smaller devices */
    margin: 0 auto;   /* center horizontally */
  }
}



  body::after {
    content: "";
    position: fixed;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    background-color: rgba(0, 0, 0, 0.2);
    z-index: -1;
  }

  /* Main container */
  .container {
    padding-top: 4rem;
    padding-bottom: 4rem;
    flex: 1;
  }

  /* Headings */
  h2 {
    font-weight: 800;
    font-size: 3rem;
    color: #fff;
    text-shadow: 2px 2px 6px rgba(0, 0, 0, 0.85);
  }

  /* Labels and paragraphs */
  label,
  p {
    font-weight: 600;
    font-size: 1.3rem;
    color: #f0f0f0;
    text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.85);
  }

  /* Form controls */
  .form-control {
    font-size: 1rem;
    font-weight: 500;
  }

  /* Submit button */
  .btn {
    font-weight: 600;
    font-size: 1.5rem;
    padding: 0.6rem;
  }

  /* Quote section for signup */
  .signup-quote {
    margin-top: 3rem;
    text-align: center;
    color: #eee;
    font-style: italic;
    font-size: 1.5rem;
    text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.7);
  }

  .quote-mark {
    font-size: 2rem;
    color: #ffc107;
  }
</style>


<!-- Sign Up Form -->
<div class="col-md-6 signup-box">
  <h2 class="mb-4 text-center">📝 Sign Up</h2>
  <form action="/signup" method="POST">
    <div class="mb-3">
      <label for="username" class="form-label">Full Name</label>
      <input type="text" class="form-control" id="username" name="username" required>
    </div>

    <div class="mb-3">
      <label for="user_id" class="form-label">User ID</label>
      <input type="text" class="form-control" id="user_id" name="user_id" required>
    </div>

    <div class="mb-3">
      <label for="password" class="form-label">Password</label>
      <input type="password" class="form-control" id="password" name="password" required>
    </div>

    <div class="mb-3">
      <label for="confirmPassword" class="form-label">Confirm Password</label>
      <input type="password" class="form-control" id="confirmPassword" name="confirmPassword" required>
    </div>

    <button type="submit" class="btn btn-success w-100">Create Account</button>
  </form>

  <div class="text-center mt-3">
    <p>Already have an account? <a href="/login" class="text-decoration-none fw-bold text-warning">Login here</a></p>
  </div>
</div>


  <!-- Inspirational Message -->
  <div class="signup-quote mt-5">
    <div class="quote-mark">❝</div>
    "Your journey to wellness begins with a single tap. Let’s take it together."
    <div class="quote-mark">❞</div>
    <p class="mt-3">Join the SheSync community and take control of your cycle, health, and happiness.</p>
  </div>

  <!-- Why Sign Up -->
  <div class="mt-5 text-center text-light">
    <h4 class="fw-bold mb-3">Why Join SheSync?</h4>
    <div class="row justify-content-center">
      <div class="col-md-4">
        <p>✅ Personalized PCOD tracking made simple</p>
      </div>
      <div class="col-md-4">
        <p>✅ Smart insights and cycle predictions</p>
      </div>
      <div class="col-md-4">
        <p>✅ Alerts, mood logging, and symptom tracking</p>
      </div>
      <div class="col-md-4">
        <p>✅ A supportive, private health community</p>
      </div>
      <div class="col-md-4">
        <p>✅ Visual data to understand your cycle</p>
      </div>
    </div>
  </div>

  <!-- Testimonials -->
  <div class="mt-5 text-light text-center">
    <h5 class="fw-bold mb-4">💬 What Users Say</h5>
    <p class="fst-italic">"I finally feel in sync with my body. SheSync makes everything easy and empowering."</p>
    <p class="fw-semibold">– Ritu, 23</p>

    <p class="fst-italic mt-4">"The reminders and health tips are so helpful. I don’t feel alone anymore."</p>
    <p class="fw-semibold">– Neha, 19</p>
  </div>

  <!-- Privacy Statement -->
  <div class="mt-5 text-light text-center">
    <h5 class="fw-bold mb-3">🔒 Your Privacy Matters</h5>
    <p>We respect your privacy and never share your health data without your permission.</p>
    <p>You're in control — always.</p>
  </div>
</div>
